const auth = require("./auth");
const books = require("./books");
const trainings = require("./trainings");
const statistics = require("./statistics");

module.exports = { auth, books, trainings, statistics };
