const { User } = require("./user");
const { Book } = require("./book");
const { Training } = require("./training");
const { Statistic } = require("./statistic");

module.exports = { User, Book, Training, Statistic };
